<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\src\Tckr.MockExchange\Tckr.MockExchange.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The test project drives ProcessBuffer/BuildReport/DetermineExitCode/BuildTopSymbols directly
         with synthetic input rather than over a real socket; those members are internal rather than
         public for exactly this reason. -->
    <InternalsVisibleTo Include="Tckr.FeedProbe.Tests"/>
  </ItemGroup>

</Project>
